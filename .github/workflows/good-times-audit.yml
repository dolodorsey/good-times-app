name: Good Times audit checks

on:
  pull_request:
    paths:
      - 'src/**'
      - 'scripts/**'
      - 'package.json'
      - 'package-lock.json'
      - 'vite.config.*'
      - '.github/workflows/good-times-audit.yml'
      - 'supabase/migrations/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.ref_name }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Apply counted verified logic changes
        run: node scripts/apply-verified-app-fixes.mjs
      - name: Refresh dependency tree and lockfile
        run: npm install --ignore-scripts
      - name: Commit generated app and lockfile changes
        if: github.event_name == 'pull_request'
        run: |
          if git diff --quiet -- src/App.jsx package-lock.json; then
            echo "Generated files are current"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/App.jsx package-lock.json
            git commit -m "fix: use local dates and render five-star ratings"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi
      - run: node --test scripts/gt-auth-session.test.mjs scripts/direct-request-validation.test.mjs
      - run: npm run build
      - name: Capture dependency audit
        run: npm audit --json > npm-audit.json || true
      - name: Upload dependency diagnosis
        uses: actions/upload-artifact@v4
        with:
          name: good-times-dependency-diagnosis
          path: npm-audit.json
          retention-days: 7
